.page {
  min-height: 100vh;
  padding: 20rpx 24rpx calc(140rpx + env(safe-area-inset-bottom));
  background: #f0f2f8;
  box-sizing: border-box;
}
.toolbar {
  display: flex;
  gap: 16rpx;
  margin-bottom: 20rpx;
}
.chip {
  padding: 10rpx 22rpx;
  border-radius: 999rpx;
  background: #fff;
  color: #6b7280;
  font-size: 24rpx;
  border: 1rpx solid #e5e7eb;
}
.chip.on {
  background: #1a1a2e;
  color: #c9a227;
  border-color: #1a1a2e;
}
.login-tip {
  margin-top: 80rpx;
  text-align: center;
  color: #666;
}
.login-tip .btn {
  margin-top: 28rpx;
  background: #1a1a2e;
  color: #c9a227;
}
.empty,
.loading {
  text-align: center;
  color: #9ca3af;
  padding: 80rpx 0;
  font-size: 26rpx;
}
.card {
  background: #fff;
  border-radius: 16rpx;
  padding: 24rpx;
  margin-bottom: 16rpx;
  border: 1rpx solid transparent;
  box-shadow: 0 4rpx 16rpx rgba(26, 26, 46, 0.04);
}
.card.fav {
  background: linear-gradient(180deg, #fff9e8 0%, #ffffff 48%);
  border-color: #e8c547;
  box-shadow: 0 6rpx 20rpx rgba(201, 162, 39, 0.18);
}
.card-hd {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: 12rpx;
  margin-bottom: 12rpx;
}
.badges {
  display: flex;
  flex-wrap: wrap;
  gap: 8rpx;
  flex: 1;
}
.tag {
  font-size: 20rpx;
  padding: 4rpx 10rpx;
  border-radius: 6rpx;
  background: #f3f4f6;
  color: #6b7280;
}
.fav-tag {
  background: #c9a227;
  color: #1a1a2e;
  font-weight: 700;
}
.code {
  background: #eef2ff;
  color: #4338ca;
}
.time {
  font-size: 22rpx;
  color: #9ca3af;
  white-space: nowrap;
}
.title {
  display: block;
  font-size: 30rpx;
  font-weight: 650;
  color: #111827;
  line-height: 1.45;
}
.card.fav .title {
  color: #1a1a2e;
  font-weight: 750;
}
.summary {
  margin-top: 10rpx;
  font-size: 24rpx;
  color: #6b7280;
  line-height: 1.55;
  overflow: hidden;
  display: -webkit-box;
  -webkit-line-clamp: 3;
  -webkit-box-orient: vertical;
}
.card-ft {
  margin-top: 14rpx;
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 12rpx;
  font-size: 22rpx;
  color: #9ca3af;
}
.expand-hint {
  color: #c9a227;
  font-weight: 600;
  white-space: nowrap;
}

/* 详情 */
.detail-card {
  background: #fff;
  border-radius: 20rpx;
  padding: 28rpx;
  box-shadow: 0 6rpx 24rpx rgba(26, 26, 46, 0.06);
}
.detail-hd {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: 12rpx;
  margin-bottom: 16rpx;
}
.detail-title {
  font-size: 36rpx;
  font-weight: 700;
  color: #1a1a2e;
  line-height: 1.45;
}
.detail-meta {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
  margin: 16rpx 0 24rpx;
  font-size: 22rpx;
  color: #9ca3af;
}
.detail-content {
  font-size: 28rpx;
  line-height: 1.75;
  color: #374151;
  white-space: pre-wrap;
  word-break: break-word;
  margin-bottom: 28rpx;
}
.detail-actions {
  display: flex;
  flex-direction: column;
  gap: 14rpx;
  margin-bottom: 16rpx;
}
.btn-action {
  margin: 0;
  background: #fff;
  color: #1a1a2e;
  border: 1px solid #c9a227;
  border-radius: 12rpx;
  font-size: 28rpx;
  font-weight: 600;
}
.btn-action::after { border: none; }
.btn-action.primary {
  background: linear-gradient(135deg, #e8c547, #c9a227);
  border-color: transparent;
}
.btn-back {
  margin: 0;
  background: #f3f5fa;
  color: #6b7280;
  border: none;
  border-radius: 12rpx;
  font-size: 28rpx;
}
.btn-back::after { border: none; }
